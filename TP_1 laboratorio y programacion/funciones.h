#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED
#endif

/** \brief solicita el operando uno y lo retorna
 * \return Operando uno ingresado por el usuario
 *
 */

float ingresar1_operando(float);

/** \brief Solicita el operando dos y lo retorna
 * \return Operando dos ingresado por el usuario
 *
 */

float ingresar2_operando(float);

/** \brief Recibe el operando uno y el operando dos y calcula la suma
 *  \param A es el Primero operando ingresado por el usuario
 *  \param B es el Segundo operando ingresado por el usuario
 * \return resultado de la Suma
 *
 */

 float suma(float A,float B);

/** \brief Recibe el operando uno y el operando dos y calcula la resta
*  \param A es el Primero operando ingresado por el usuario
*  \param B es el Segundo operando ingresado por el usuario
* \return resultado de la Resta
*
*/

float resta(float A,float B);

/** \brief Recibe el operando uno y el operando dos y calcula la division
 *  \param A es el Primero operando ingresado por el usuario
 *  \param B es el Segundo operando ingresado por el usuario
 * \return Resultado de la Division
 *
 */

 float division(float A,float B);


/** \brief Recibe el operando uno y el operando dos y calcula la multiplicación
 *  \param A es el Primero operando ingresado por el usuario
 *  \param B es el Segundo operando ingresado por el usuario
 * \return Resultado de la Multiplicación
 *
 */

 float multiplicacion(float A,float B);


 /** \brief Recibe el operando uno y calcula el factorial del mismo
  * \param A es el primer operando ingresado por el usuario
  * \param I es el contador necesario para poder calcular el factorial
  * \return Resultado del Factorial
  *
  */

  int factorial(int A);








